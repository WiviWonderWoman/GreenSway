import React from "react";
/** 
 * Loading is a stateless component for showing a loader while content from API is loading
 * @component  
 */
export default class Loading extends React.Component {
    render() {
        return (
            <div className="loader"></div>
        )
    }
}